/.venv/
*.unw
*.unw.ers
*npy
*.egg-info/
.pytest_cache/

# run artifacts (checkpoints, pickled test sets, reconstructions, shapefiles)
/models
/outputs/
/test_data/
/out_polygs/

# locally downloaded patches and the train-ready tree derived from them
/data/

# LSF scheduler stdout/stderr from scripts/ (the directory itself stays tracked)
/logs/*
!/logs/.gitkeep

.DS_Store
._*
__pycache__/
